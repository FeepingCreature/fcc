module test143;

import std.smtp, std.process;

void main() {
  if (getenv("TESTSUITE")) return;
  sendmail(
    host => "smtp.example.com",
    logindata => "user:pass",
    from => "user",
    to => "to",
    subject => "this is a test",
    content => "hello world! :D",
    dbg => true
  );
}
